import React from 'react';
import styles from './home.scss';
import Logo from '../components/Logo/Logo';

const Home = () => (
    <section>
        <Logo></Logo>
    </section>
)

export default Home;